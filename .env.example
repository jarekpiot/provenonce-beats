# Solana RPC (devnet for testing, mainnet-beta for production)
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Anchor wallet — writes global beat anchor memos to Solana
# Generate: node -e "const{Keypair}=require('@solana/web3.js');const k=Keypair.generate();console.log('Address:',k.publicKey.toBase58());const bs58=require('bs58');console.log('Secret:',bs58.encode(k.secretKey))"
BEATS_ANCHOR_KEYPAIR=your_base58_anchor_secret_key_here

# Cron secret — protects the anchor cron endpoint (auto-set by Vercel)
# CRON_SECRET=auto_generated_by_vercel
